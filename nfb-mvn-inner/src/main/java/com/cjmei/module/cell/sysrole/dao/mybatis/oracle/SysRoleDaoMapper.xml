<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjmei.module.cell.sysrole.dao.SysRoleDao">

	<select id="findById" resultType="com.cjmei.module.cell.sysrole.pojo.SysRole">
		SELECT * FROM TB_SYS_ROLE
		sysrole where sysrole.ROLEID = #{roleid}
	</select>

	<insert id="save" parameterType="com.cjmei.module.cell.sysrole.pojo.SysRole">
		INSERT INTO TB_SYS_ROLE sysrole (
		sysrole.ROLEID,
		sysrole.ORGID,
		sysrole.UPDATETIME,
		sysrole.CREATOR,
		sysrole.ROLENAME,
		sysrole.CREATETIME
		)
		VALUES(
		#{roleid:VARCHAR},
		#{orgid:VARCHAR},
		#{updatetime:TIMESTAMP},
		#{creator:VARCHAR},
		#{rolename:VARCHAR},
		#{createtime:TIMESTAMP}

		)
	</insert>

	<update id="update" parameterType="com.cjmei.module.cell.sysrole.pojo.SysRole">
		UPDATE TB_SYS_ROLE sysrole
		SET
		sysrole.ORGID = #{orgid:VARCHAR},
		sysrole.UPDATETIME =
		#{updatetime:TIMESTAMP},
		sysrole.CREATOR = #{creator:VARCHAR},
		sysrole.ROLENAME = #{rolename:VARCHAR},
		sysrole.CREATETIME =
		#{createtime:TIMESTAMP}
		WHERE
		sysrole.ROLEID = #{roleid}
	</update>

	<delete id="delete" parameterType="String">
		DELETE FROM TB_SYS_ROLE
		sysrole WHERE sysrole.ROLEID=#{roleid}
	</delete>

	<select id="findAll" resultType="com.cjmei.module.cell.sysrole.pojo.SysRole">
		SELECT * FROM TB_SYS_ROLE
	</select>

	<delete id="deleteBatch" parameterType="java.util.List">
		DELETE FROM TB_SYS_ROLE WHERE ROLEID in
		<foreach collection="list" item="ids" index="index" open="("
			separator="," close=")">
			#{ids}
		</foreach>
	</delete>

	<select id="list" resultType="com.cjmei.module.cell.sysrole.pojo.SysRole"
		parameterType="java.util.Map">
		SELECT *
		FROM TB_SYS_ROLE sysrole
		WHERE 1=1
		and ( sysrole.ROLEID LIKE
		CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
		<if test="keyword!=null and keyword!=''">
			or sysrole.ORGID LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
		</if>
		<if test="keyword!=null and keyword!=''">
			or sysrole.UPDATETIME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
		</if>
		<if test="keyword!=null and keyword!=''">
			or sysrole.CREATOR LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
		</if>
		<if test="keyword!=null and keyword!=''">
			or sysrole.ROLENAME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
		</if>
		<if test="keyword!=null and keyword!=''">
			or sysrole.CREATETIME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
		</if>
		)


	</select>

	<insert id="saveRoleFunc" parameterType="com.cjmei.module.system.sys.pojo.SysRoleFunc">
		INSERT INTO TB_SYS_ROLE_FUNC sysrolefunc (
		sysrolefunc.ROLEID,
		sysrolefunc.FUNCID,
		sysrolefunc.UPDATETIME,
		sysrolefunc.CREATOR,
		sysrolefunc.CREATETIME
		)
		VALUES(
		#{roleid:VARCHAR},
		#{funcid:VARCHAR},
		#{updatetime:TIMESTAMP},
		#{creator:VARCHAR},
		#{createtime:TIMESTAMP}

		)
	</insert>
	<delete id="deleteFuncByRoleid">
		delete from tb_sys_role_func t where t.roleid=#{roleid}
	</delete>

</mapper>