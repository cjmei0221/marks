<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.marks.module.inner.user.sysrole.dao.SysRoleDao">

	<select id="findById" resultType="com.marks.module.inner.user.sysrole.pojo.SysRole">
		SELECT * FROM TB_SYS_ROLE
		sysrole where sysrole.ROLEID = #{roleid}
	</select>

	<insert id="save" parameterType="com.marks.module.inner.user.sysrole.pojo.SysRole">
		INSERT INTO TB_SYS_ROLE
		(
		ROLEID,
		USERTYPE,
		LVL,
		showflag,
		UPDATETIME,
		CREATOR,
		ROLENAME,
		CREATETIME,
		COMPANYID
		)
		VALUES(
		#{roleid:VARCHAR},
		#{userType:VARCHAR},
		#{lvl:NUMERIC},
		#{showFlag:NUMERIC},
		now(),
		#{creator:VARCHAR},
		#{rolename:VARCHAR},
		now(),
		#{companyId:VARCHAR}
		)
	</insert>

	<update id="update" parameterType="com.marks.module.inner.user.sysrole.pojo.SysRole">
		UPDATE TB_SYS_ROLE 
		SET
		USERTYPE = #{userType:VARCHAR},
		LVL=#{lvl:NUMERIC},
		COMPANYID=#{companyId:VARCHAR},
		showflag=#{showFlag:NUMERIC},
		UPDATETIME =
		now(),
		ROLENAME =
		#{rolename:VARCHAR}
		WHERE
		ROLEID = #{roleid}
	</update>

	<delete id="delete" parameterType="String">
		DELETE FROM TB_SYS_ROLE
		 WHERE ROLEID=#{roleid}
	</delete>

	<select id="findAll" resultType="com.marks.module.inner.user.sysrole.pojo.SysRole">
		SELECT * FROM TB_SYS_ROLE
	</select>

	<delete id="deleteBatch" parameterType="java.util.List">
		DELETE FROM TB_SYS_ROLE WHERE ROLEID in
		<foreach collection="list" item="ids" index="index" open="("
			separator="," close=")">
			#{ids}
		</foreach>
	</delete>
	
	<select id="getUserlist" resultType="com.marks.module.inner.user.sysrole.pojo.SysRole"
		parameterType="java.util.Map">
		SELECT sysrole.*,o.orgname companyName
		FROM TB_SYS_ROLE sysrole left join
		tb_org_info o on sysrole.companyId=o.orgid
		WHERE 1=1
		<if test="companyId !=null and companyId != ''">
			and sysrole.COMPANYID = #{companyId}
		</if>
		and sysrole.lvl &gt;= #{lvl}
		and sysrole.SHOWFLAG=1
		ORDER BY sysrole.lvl
	</select>

	<select id="list" resultType="com.marks.module.inner.user.sysrole.pojo.SysRole"
		parameterType="java.util.Map">
		SELECT sysrole.*,o.orgname companyName
		FROM TB_SYS_ROLE sysrole left join
		tb_org_info o on sysrole.companyId=o.orgid
		WHERE 1=1
		<if test="companyId !=null and companyId != ''">
			and sysrole.COMPANYID = #{companyId}
		</if>
		<if test="keyword!=null and keyword!=''">
			and ( sysrole.ROLEID
			LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
			or sysrole.ORGID LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
			or
			sysrole.UPDATETIME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or sysrole.CREATOR LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or sysrole.ROLENAME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or sysrole.CREATETIME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			)
		</if>
		<if test="s_lvl !=null and s_lvl != ''">
			and sysrole.lvl = #{s_lvl}
		</if>
		and sysrole.lvl &gt;= #{lvl}

		ORDER BY sysrole.UPDATETIME desc

	</select>

	<insert id="saveRoleFunc" parameterType="com.marks.module.inner.user.sysrole.pojo.SysRoleFunc">
		INSERT INTO
		TB_SYS_ROLE_FUNC  (
		ROLEID,
		FUNCID,
		UPDATETIME,
		CREATOR,
		CREATETIME
		)
		VALUES(
		#{roleid:VARCHAR},
		#{funcid:VARCHAR},
		now(),
		#{creator:VARCHAR},
		now()
		)
	</insert>
	<delete id="deleteFuncByRoleid">
		delete from tb_sys_role_func  where
		roleid=#{roleid}
	</delete>
	<select id="getFuncList" resultType="com.marks.module.inner.system.sys.pojo.SysFunc">
		SELECT distinct
		VSRF.OPERID,
		VSRF.OPERNAME,
		vsrf.sort,
		vsrf.MENUID,
		vsrf.FUNCID,
		t2.state
		FROM
		VIEW_SYS_ROLE_FUNC vsrf
		left join (select
		DISTINCT tsrf.funcid, 'checked' state
		from tb_sys_role_func tsrf where
		tsrf.roleid=#{roleId:VARCHAR}
		) t2 on t2.funcid = vsrf.funcid
		where 1=1
		<if test="loginUserRoleId != null">
			AND vsrf.ROLEID =#{loginUserRoleId}
		</if>
		order by vsrf.sort
	</select>
	<select id="findByUserTypeAndCompanyId" resultType="com.marks.module.inner.user.sysrole.pojo.SysRole">
		select * from
		tb_sys_role t where t.roleid=#{roleid} and
		t.companyid=#{companyId}
	</select>
	<select id="countUserByRoleid" resultType="int">
		select count(1) from
		tb_user t where t.roleid=#{roleId}
	</select>
</mapper>