<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjmei.module.wx.wxtemplate.dao.WxTemplateDao">

    <select id="findById" resultType="com.cjmei.module.wx.wxtemplate.pojo.WxTemplate">
		SELECT * FROM TB_WX_TEMPLATE where YWTYPE = #{ywType}
 	</select>
 	
 	<insert id="save" parameterType="com.cjmei.module.wx.wxtemplate.pojo.WxTemplate">
		<selectKey keyProperty="ywType" resultType="String" order="BEFORE">select replace(uuid(),'-','')</selectKey>

 		INSERT INTO TB_WX_TEMPLATE (
            YWTYPE,
            ACCOUNTID,
            TEMPLATE_ID,
            TEMPLATE_NAME,
            FIRST_CONTENT,
            REMARK_CONTENT,
            DETAILURL,
            STATUS,
            CREATETIME,
            UPDATETIME,
            CREATOR
		)
		VALUES(
            #{ywType:VARCHAR},
            #{accountid:VARCHAR},
            #{template_id:VARCHAR},
            #{template_name:VARCHAR},
            #{first_content:VARCHAR},
            #{remark_content:VARCHAR},
            #{detailUrl:VARCHAR},
            #{status:NUMERIC},
            #{createtime:TIMESTAMP},
            #{updatetime:TIMESTAMP},
            #{creator:VARCHAR}
			
			)
 	</insert>
 	
 	<update id="update" parameterType="com.cjmei.module.wx.wxtemplate.pojo.WxTemplate">
 		UPDATE TB_WX_TEMPLATE 
		SET 
                ACCOUNTID = #{accountid:VARCHAR},
                TEMPLATE_ID = #{template_id:VARCHAR},
                TEMPLATE_NAME = #{template_name:VARCHAR},
                FIRST_CONTENT = #{first_content:VARCHAR},
                REMARK_CONTENT = #{remark_content:VARCHAR},
                DETAILURL = #{detailUrl:VARCHAR},
                STATUS = #{status:NUMERIC},
                CREATETIME = #{createtime:TIMESTAMP},
                UPDATETIME = #{updatetime:TIMESTAMP},
                CREATOR = #{creator:VARCHAR}
		WHERE
			YWTYPE = #{ywType}
 	</update>
 	
 	<delete id="delete" parameterType="String">
 		DELETE FROM TB_WX_TEMPLATE WHERE YWTYPE=#{ywType}
 	</delete>
 	
 	<select id="findAll" resultType="com.cjmei.module.wx.wxtemplate.pojo.WxTemplate">
 		SELECT * FROM TB_WX_TEMPLATE
 	</select>
 	
 	<delete id="deleteBatch" parameterType="java.util.List">
 		DELETE FROM TB_WX_TEMPLATE WHERE YWTYPE in 
 		<foreach collection="list" item="ids" index="index" open="(" separator="," close=")">
				#{ids}
		</foreach>
 	</delete>
 	
 	<select id="list" resultType="com.cjmei.module.wx.wxtemplate.pojo.WxTemplate" 
 	parameterType="java.util.Map">
 		SELECT  *
		  FROM   TB_WX_TEMPLATE 
						WHERE 1=1
                        <if test="keyword!=null and keyword!=''">
 and ( wxtemplate.YWTYPE LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.ACCOUNTID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.TEMPLATE_ID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.TEMPLATE_NAME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.FIRST_CONTENT LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.REMARK_CONTENT LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.DETAILURL LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.STATUS LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.CREATETIME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.UPDATETIME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxtemplate.CREATOR LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
 )                        </if>

						
					
 	</select>
 	
 	
 	
</mapper>