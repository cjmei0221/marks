<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.marks.module.mall.advise.dao.AdviseDao">

    <select id="findById" resultType="com.marks.module.mall.advise.pojo.Advise">
		SELECT * FROM TB_MALL_ADVISE advise where advise.ID = #{ID}
 	</select>
 	
 	<insert id="save" parameterType="com.marks.module.mall.advise.pojo.Advise">
		<selectKey keyProperty="ID" resultType="String" order="BEFORE">select sys_guid() from dual</selectKey>

 		INSERT INTO TB_MALL_ADVISE advise (
            advise.ID,
            advise.CONTENT,
            advise.LABEL,
            advise.NUM,
            advise.CREATETIME,
            advise.UPDATETIME,
            advise.USERID,
            advise.MOBILE
		)
		VALUES(
            #{ID:VARCHAR},
            #{content:VARCHAR},
            #{label:VARCHAR},
            #{num:NUMERIC},
            sysdate,
            sysdate,
            #{userid:VARCHAR},
            #{mobile:VARCHAR}
			
			)
 	</insert>
 	
 	<update id="update" parameterType="com.marks.module.mall.advise.pojo.Advise">
 		UPDATE TB_MALL_ADVISE advise
		SET 
                advise.CONTENT = #{content:VARCHAR},
                advise.LABEL = #{label:VARCHAR},
                advise.NUM = #{num:NUMERIC},
                advise.UPDATETIME = sysdate,
                advise.USERID = #{userid:VARCHAR},
                advise.MOBILE = #{mobile:VARCHAR}
		WHERE
			advise.ID = #{ID}
 	</update>
 	
 	<delete id="delete" parameterType="String">
 		DELETE FROM TB_MALL_ADVISE advise WHERE advise.ID=#{ID}
 	</delete>
 	
 	<select id="findAll" resultType="com.marks.module.mall.advise.pojo.Advise">
 		SELECT * FROM TB_MALL_ADVISE
 	</select>
 	
 	<delete id="deleteBatch" parameterType="java.util.List">
 		DELETE FROM TB_MALL_ADVISE WHERE ID in 
 		<foreach collection="list" item="ids" index="index" open="(" separator="," close=")">
				#{ids}
		</foreach>
 	</delete>
 	
 	<select id="list" resultType="com.marks.module.mall.advise.pojo.Advise" 
 	parameterType="java.util.Map">
 		SELECT  *
		  FROM   TB_MALL_ADVISE advise
						WHERE 1=1
 and ( advise.ID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        <if test="keyword!=null and keyword!=''">
                            or advise.CONTENT LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        </if>
                        <if test="keyword!=null and keyword!=''">
                            or advise.LABEL LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        </if>
                        <if test="keyword!=null and keyword!=''">
                            or advise.NUM LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        </if>
                        <if test="keyword!=null and keyword!=''">
                            or advise.CREATETIME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        </if>
                        <if test="keyword!=null and keyword!=''">
                            or advise.UPDATETIME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        </if>
                        <if test="keyword!=null and keyword!=''">
                            or advise.USERID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        </if>
                        <if test="keyword!=null and keyword!=''">
                            or advise.MOBILE LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                        </if>
 )
					
					
 	</select>
 	
 	
 	
</mapper>