<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjmei.module.note.transaction.dao.TransactionDao">

    <select id="findById" resultType="com.cjmei.module.note.transaction.pojo.Transaction">
		SELECT * FROM TB_NOTE_TRANSACTION where ID = #{id}
 	</select>
 	
 	<insert id="save" parameterType="com.cjmei.module.note.transaction.pojo.Transaction">
		<selectKey keyProperty="id" resultType="String" order="BEFORE">select replace(uuid(),'-','')</selectKey>

 		INSERT INTO TB_NOTE_TRANSACTION (
            ID,
            TRANTYPE,
            TRANDATE,
            ISREPEAT,
            TRANCONTENT,
            AHEADDAYS,
            TRANTIME,
            ISAHEAD,
            CREATETIME,
            UPDATETIME,
            CREATOR
		)
		VALUES(
            #{id:VARCHAR},
            #{tranType:NUMERIC},
            #{tranDate:TIMESTAMP},
            #{isRepeat:NUMERIC},
            #{tranContent:VARCHAR},
            #{aheadDays:NUMERIC},
            #{tranTime:VARCHAR},
            #{isAhead:NUMERIC},
            #{createtime:TIMESTAMP},
            #{updatetime:TIMESTAMP},
            #{creator:VARCHAR}
			
			)
 	</insert>
 	
 	<update id="update" parameterType="com.cjmei.module.note.transaction.pojo.Transaction">
 		UPDATE TB_NOTE_TRANSACTION 
		SET 
                TRANTYPE = #{tranType:NUMERIC},
                TRANDATE = #{tranDate:TIMESTAMP},
                ISREPEAT = #{isRepeat:NUMERIC},
                TRANCONTENT = #{tranContent:VARCHAR},
                AHEADDAYS = #{aheadDays:NUMERIC},
                TRANTIME = #{tranTime:VARCHAR},
                ISAHEAD = #{isAhead:NUMERIC},
                CREATETIME = #{createtime:TIMESTAMP},
                UPDATETIME = #{updatetime:TIMESTAMP},
                CREATOR = #{creator:VARCHAR}
		WHERE
			ID = #{id}
 	</update>
 	
 	<delete id="delete" parameterType="String">
 		DELETE FROM TB_NOTE_TRANSACTION WHERE ID=#{id}
 	</delete>
 	
 	<select id="findAll" resultType="com.cjmei.module.note.transaction.pojo.Transaction">
 		SELECT * FROM TB_NOTE_TRANSACTION
 	</select>
 	
 	<delete id="deleteBatch" parameterType="java.util.List">
 		DELETE FROM TB_NOTE_TRANSACTION WHERE ID in 
 		<foreach collection="list" item="ids" index="index" open="(" separator="," close=")">
				#{ids}
		</foreach>
 	</delete>
 	
 	<select id="list" resultType="com.cjmei.module.note.transaction.pojo.Transaction" 
 	parameterType="java.util.Map">
 		SELECT  *
		  FROM   TB_NOTE_TRANSACTION 
						WHERE 1=1
                        <if test="keyword!=null and keyword!=''">
 and ( transaction.ID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or transaction.TRANTYPE LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or to_char(transaction.TRANDATE,'yyyy-MM-dd') LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or transaction.ISREPEAT LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or transaction.TRANCONTENT LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or transaction.AHEADDAYS LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or transaction.TRANTIME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or transaction.ISAHEAD LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or to_char(transaction.CREATETIME,'yyyy-MM-dd') LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or to_char(transaction.UPDATETIME,'yyyy-MM-dd') LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or transaction.CREATOR LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
 )                        </if>

						
					
 	</select>
 	
 	
 	
</mapper>