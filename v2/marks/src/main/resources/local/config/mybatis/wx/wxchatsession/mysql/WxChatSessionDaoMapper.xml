<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.marks.module.wx.wxchatsession.dao.WxChatSessionDao">

    <select id="findById" resultType="com.marks.module.wx.wxchatsession.pojo.WxChatSession">
		SELECT * FROM TB_WX_CHAT_SESSION where SESSION_ID = #{session_id}
 	</select>
 	
 	<insert id="save" parameterType="com.marks.module.wx.wxchatsession.pojo.WxChatSession">
		<selectKey keyProperty="session_id" resultType="String" order="BEFORE">select replace(uuid(),'-','')</selectKey>

 		INSERT INTO TB_WX_CHAT_SESSION (
            SESSION_ID,
            ACCOUNTID,
            OPENID,
            CREATETIME,
            CREATELONG,
            UPDATETIME,
            C_CONTENT,
            ENDTIME,
            FANID
		)
		VALUES(
            #{session_id:VARCHAR},
            #{accountid:VARCHAR},
            #{openid:VARCHAR},
            now(),
            #{createLong:NUMERIC},
            now(),
            #{c_content:VARCHAR},
            #{endtime:TIMESTAMP},
            #{fanId:VARCHAR}
			
			)
 	</insert>
 	
 	<update id="update" parameterType="com.marks.module.wx.wxchatsession.pojo.WxChatSession">
 		UPDATE TB_WX_CHAT_SESSION 
		SET 
                ACCOUNTID = #{accountid:VARCHAR},
                OPENID = #{openid:VARCHAR},
                CREATELONG = #{createLong:NUMERIC},
                UPDATETIME = now(),
                C_CONTENT = #{c_content:VARCHAR},
                ENDTIME = #{endtime:TIMESTAMP},
                FANID = #{fanId:VARCHAR}
		WHERE
			SESSION_ID = #{session_id}
 	</update>
 	
 	<delete id="delete" parameterType="String">
 		DELETE FROM TB_WX_CHAT_SESSION WHERE SESSION_ID=#{session_id}
 	</delete>
 	
 	<select id="findAll" resultType="com.marks.module.wx.wxchatsession.pojo.WxChatSession">
 		SELECT * FROM TB_WX_CHAT_SESSION
 	</select>
 	
 	<delete id="deleteBatch" parameterType="java.util.List">
 		DELETE FROM TB_WX_CHAT_SESSION WHERE SESSION_ID in 
 		<foreach collection="list" item="ids" index="index" open="(" separator="," close=")">
				#{ids}
		</foreach>
 	</delete>
 	
 	<select id="list" resultType="com.marks.module.wx.wxchatsession.pojo.WxChatSession" 
 	parameterType="java.util.Map">
 		SELECT  *
		  FROM   TB_WX_CHAT_SESSION 
						WHERE 1=1
                        <if test="keyword!=null and keyword!=''">
 and ( wxchatsession.SESSION_ID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxchatsession.ACCOUNTID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxchatsession.OPENID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or to_char(wxchatsession.CREATETIME,'yyyy-MM-dd') LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxchatsession.CREATELONG LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or to_char(wxchatsession.UPDATETIME,'yyyy-MM-dd') LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxchatsession.C_CONTENT LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or to_char(wxchatsession.ENDTIME,'yyyy-MM-dd') LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or wxchatsession.FANID LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
 )                        </if>

						
			    order by  wxchatsession.UPDATETIME DESC		
 	</select>
 	
 	
 	
</mapper>