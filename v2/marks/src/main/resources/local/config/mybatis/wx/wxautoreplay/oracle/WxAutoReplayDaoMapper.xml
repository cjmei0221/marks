<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.marks.module.wx.wxautoreplay.dao.WxAutoReplayDao">

	<select id="findById" resultType="com.marks.module.wx.wxautoreplay.pojo.WxAutoReplay">
		SELECT * FROM TB_WX_AUTO_REPLAY
		wxautoreplay where wxautoreplay.CTYPE = #{ctype:VARCHAR}
	</select>

	<select id="findByCkey" resultType="com.marks.module.wx.wxautoreplay.pojo.WxAutoReplay">
		SELECT * FROM
		TB_WX_AUTO_REPLAY
		wxautoreplay where
		wxautoreplay.ACCOUNTID=#{accountid} and wxautoreplay.CKEY=#{ckey}
	</select>

	<insert id="save" parameterType="com.marks.module.wx.wxautoreplay.pojo.WxAutoReplay">
		<selectKey keyProperty="ctype" resultType="String" order="BEFORE">select
			sys_guid() from dual</selectKey>

		INSERT INTO TB_WX_AUTO_REPLAY wxautoreplay (
		wxautoreplay.CKEY,
		wxautoreplay.CREPLAY,
		wxautoreplay.CTYPE,
		wxautoreplay.CTYPENAME,
		wxautoreplay.ACCOUNTID,
		wxautoreplay.REPLAYTYPE,
		wxautoreplay.CREATETIME,
		wxautoreplay.UPDATETIME,
		wxautoreplay.CREATOR,
		wxautoreplay.DELFLAG,
		wxautoreplay.CKEYNAME
		)
		VALUES(
		#{ckey:VARCHAR},
		#{creplay:VARCHAR},
		#{ctype:VARCHAR},
		#{ctypeName:VARCHAR},
		#{accountid:VARCHAR},
		#{replayType:VARCHAR},
		sysdate,
		sysdate,
		#{creator:VARCHAR},
		#{delFlag:NUMERIC},
		#{ckeyName:VARCHAR}
		)
	</insert>

	<update id="update" parameterType="com.marks.module.wx.wxautoreplay.pojo.WxAutoReplay">
		UPDATE TB_WX_AUTO_REPLAY
		wxautoreplay
		SET
		wxautoreplay.CKEY = #{ckey:VARCHAR},
		wxautoreplay.CREPLAY = #{creplay:VARCHAR},
		wxautoreplay.CTYPENAME =
		#{ctypeName:VARCHAR},
		wxautoreplay.REPLAYTYPE = #{replayType:VARCHAR},
		wxautoreplay.UPDATETIME = sysdate,
		wxautoreplay.DELFLAG=#{delFlag:NUMERIC},
		wxautoreplay.CKEYNAME=#{ckeyName:VARCHAR}
		WHERE
		wxautoreplay.CTYPE =
		#{ctype:VARCHAR}
	</update>

	<delete id="delete" parameterType="String">
		DELETE FROM TB_WX_AUTO_REPLAY
		wxautoreplay WHERE
		wxautoreplay.CTYPE = #{ctype:VARCHAR}
	</delete>

	<select id="findAll" resultType="com.marks.module.wx.wxautoreplay.pojo.WxAutoReplay">
		SELECT * FROM TB_WX_AUTO_REPLAY
	</select>

	<delete id="deleteBatch" parameterType="java.util.List">
		DELETE FROM TB_WX_AUTO_REPLAY WHERE CTYPE in
		<foreach collection="list" item="ids" index="index" open="("
			separator="," close=")">
			#{ids}
		</foreach>
	</delete>

	<select id="list" resultType="com.marks.module.wx.wxautoreplay.pojo.WxAutoReplay"
		parameterType="java.util.Map">
		SELECT *
		FROM TB_WX_AUTO_REPLAY wxautoreplay
		WHERE 1=1
		<if test="accountIds !=null">
			and wxautoreplay.accountid in
			<foreach collection="accountIds" item="oid" index="index"
				open="(" separator="," close=")">
				#{oid}
			</foreach>
		</if>
		<if test="keyword!=null and keyword!=''">
			and ( wxautoreplay.CKEY LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or wxautoreplay.CREPLAY LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or wxautoreplay.CTYPENAME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or wxautoreplay.ACCOUNTID LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or wxautoreplay.REPLAYTYPE LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')

			or wxautoreplay.CKEYNAME LIKE
			CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
			)
		</if>

	</select>



</mapper>