<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjmei.module.system.datadir.dao.DataDirDao">

    <select id="findById" resultType="com.cjmei.module.system.datadir.pojo.DataDir">
		SELECT * FROM TB_SYS_DATADIR_INFO where CKEY = #{ckey}
 	</select>
 	
 	<insert id="save" parameterType="com.cjmei.module.system.datadir.pojo.DataDir">
		<selectKey keyProperty="ckey" resultType="String" order="BEFORE">select replace(uuid(),'-','')</selectKey>

 		INSERT INTO TB_SYS_DATADIR_INFO (
            CKEY,
            PARENTKEY,
            CVALUE,
            TYPENAME,
            SORT,
            CREATETIME,
            UPDATETIME,
            CREATOR
		)
		VALUES(
            #{ckey:VARCHAR},
            #{parentkey:VARCHAR},
            #{cvalue:VARCHAR},
            #{typename:VARCHAR},
            #{sort:NUMERIC},
            #{createtime:VARCHAR},
            #{updatetime:VARCHAR},
            #{creator:VARCHAR}
			
			)
 	</insert>
 	
 	<update id="update" parameterType="com.cjmei.module.system.datadir.pojo.DataDir">
 		UPDATE TB_SYS_DATADIR_INFO 
		SET 
                PARENTKEY = #{parentkey:VARCHAR},
                CVALUE = #{cvalue:VARCHAR},
                TYPENAME = #{typename:VARCHAR},
                SORT = #{sort:NUMERIC},
                CREATETIME = #{createtime:VARCHAR},
                UPDATETIME = #{updatetime:VARCHAR},
                CREATOR = #{creator:VARCHAR}
		WHERE
			CKEY = #{ckey}
 	</update>
 	
 	<delete id="delete" parameterType="String">
 		DELETE FROM TB_SYS_DATADIR_INFO WHERE CKEY=#{ckey}
 	</delete>
 	
 	<select id="findAll" resultType="com.cjmei.module.system.datadir.pojo.DataDir">
 		SELECT * FROM TB_SYS_DATADIR_INFO
 	</select>
 	
 	<delete id="deleteBatch" parameterType="java.util.List">
 		DELETE FROM TB_SYS_DATADIR_INFO WHERE CKEY in 
 		<foreach collection="list" item="ids" index="index" open="(" separator="," close=")">
				#{ids}
		</foreach>
 	</delete>
 	
 	<select id="list" resultType="com.cjmei.module.system.datadir.pojo.DataDir" 
 	parameterType="java.util.Map">
 		SELECT  *
		  FROM   TB_SYS_DATADIR_INFO 
						WHERE 1=1
                        <if test="keyword!=null and keyword!=''">
 and ( datadir.CKEY LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or datadir.PARENTKEY LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or datadir.CVALUE LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or datadir.TYPENAME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or datadir.SORT LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or datadir.CREATETIME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or datadir.UPDATETIME LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
                            or datadir.CREATOR LIKE CONCAT(CONCAT('%',#{keyword:VARCHAR}),'%')
 )                        </if>

						
					
 	</select>
 	
 	
 	
</mapper>